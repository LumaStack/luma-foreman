# Journal — Deprecate backlog-ideas, and learn how deprecation goes

> The work item's memory. Newest entry first; everything below the top block is
> historical. Append, never curate. Shape: `spec.md` §5.5.

---

## ▶ 2026-09-24

delete-bundle step 2 relies on obligation: deprecated reaching adopters. Grepped foreman's source — Python, not Go — and it reads no obligation key and has no deprecation report at all. The catalog uses the word; the adopter-side tool never looks. So the procedure's one notification mechanism is unbuilt.
