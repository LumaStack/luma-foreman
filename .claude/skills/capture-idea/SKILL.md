---
name: capture-idea
description: Write down an idea before it is lost, check whether it already exists, and only then ask how much detail is worth adding now. Use when something worth doing surfaces during other work.
---

<!-- luma-foreman:generated from lumastack/luma-catalog/backlog-ideas procedure/capture-idea. Regenerate with `luma-foreman apply`; edits are lost. -->

# Capture an idea

**Read `.luma/bundles/lumastack/luma-catalog/backlog-ideas/procedure/capture-idea.md` and follow it.** That file is the procedure. This is the adapter that makes it reachable from here, and it deliberately carries no copy of it — the copy would drift.

Required reading from this bundle — open before following the procedure:

- `.luma/bundles/lumastack/luma-catalog/backlog-ideas/policy/capturing-ideas.md` — The test for whether an idea earns a file, what disqualifies one, and why capture optimises for flow rather than completeness.

From the `lumastack/luma-catalog/backlog-ideas` bundle, vendored at `.luma/bundles/lumastack/luma-catalog/backlog-ideas/`. Do not edit anything under there — an adopted bundle is a copy, and editing it is drift.
