---
name: capture-idea
description: Write down an idea before it is lost, check whether it already exists, and only then ask how much detail is worth adding now. Use when something worth doing surfaces during other work.
---

<!-- luma-foreman:generated from lumastack/luma-catalog/backlog-ideas workflows/capture-idea. Regenerate with `luma-foreman apply`; edits are lost. -->

# Capture an idea

**Read `.luma/bundles/lumastack/luma-catalog/backlog-ideas/workflows/capture-idea.md` and follow it.** That file is the workflow. This is the adapter that makes it reachable from here, and it deliberately carries no copy of it — the copy would drift.

From the `lumastack/luma-catalog/backlog-ideas` bundle, vendored at `.luma/bundles/lumastack/luma-catalog/backlog-ideas/`. Do not edit anything under there — an adopted bundle is a copy, and editing it is drift.
