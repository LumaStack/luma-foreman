---
name: migrate-ideas
description: Move ideas out of a single IDEAS.md into individual files, verify nothing was lost, and only then remove the original. Use once per project that has one.
---

<!-- luma-foreman:generated from lumastack/luma-catalog/backlog-ideas procedure/migrate-ideas. Regenerate with `luma-foreman apply`; edits are lost. -->

# Migrate an IDEAS file

**Read `.luma/bundles/lumastack/luma-catalog/backlog-ideas/procedure/migrate-ideas.md` and follow it.** That file is the procedure. This is the adapter that makes it reachable from here, and it deliberately carries no copy of it — the copy would drift.

Required reading from this bundle — open before following the procedure:

- `.luma/bundles/lumastack/luma-catalog/backlog-ideas/policy/capturing-ideas.md` — The test for whether an idea earns a file, what disqualifies one, and why capture optimises for flow rather than completeness.

From the `lumastack/luma-catalog/backlog-ideas` bundle, vendored at `.luma/bundles/lumastack/luma-catalog/backlog-ideas/`. Do not edit anything under there — an adopted bundle is a copy, and editing it is drift.
