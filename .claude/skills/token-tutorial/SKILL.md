---
name: token-tutorial
description: A paced tutorial on where an agent session's tokens actually go — presented a step at a time, pausing after each one for questions or practice, and ending in a short quiz. Use when somebody wants to learn the material rather than measure a setup.
---

<!-- luma-foreman:generated from lumastack/luma-catalog/token-manager workflows/token-tutorial. Regenerate with `luma-foreman apply`; edits are lost. -->

# Walk me through token usage best practices

**Read `.luma/bundles/lumastack/luma-catalog/token-manager/workflows/token-tutorial/WORKFLOW.md` and follow it.** That file is the workflow. This is the adapter that makes it reachable from here, and it deliberately carries no copy of it — the copy would drift.

From the `lumastack/luma-catalog/token-manager` bundle, vendored at `.luma/bundles/lumastack/luma-catalog/token-manager/`. Do not edit anything under there — an adopted bundle is a copy, and editing it is drift.
