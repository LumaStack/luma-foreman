---
name: session-handoff
description: Transfer work to another agent or a future session so it resumes without losing what this one learned. Use before a compaction, before exiting, or when someone else takes over — the work is continuing, somewhere else.
---

<!-- luma-foreman:generated from lumastack/luma-catalog/session-manager workflows/session-handoff. Regenerate with `luma-foreman apply`; edits are lost. -->

# Hand off a session

**Read `.luma/bundles/lumastack/luma-catalog/session-manager/workflows/session-handoff.md` and follow it.** That file is the workflow. This is the adapter that makes it reachable from here, and it deliberately carries no copy of it — the copy would drift.

From the `lumastack/luma-catalog/session-manager` bundle, vendored at `.luma/bundles/lumastack/luma-catalog/session-manager/`. Do not edit anything under there — an adopted bundle is a copy, and editing it is drift.
