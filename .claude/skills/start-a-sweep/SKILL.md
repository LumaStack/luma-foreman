---
name: start-a-sweep
description: Settle what is being read and what is not, choose an order, and build the index that makes coverage checkable. Use when beginning a file-by-file review of a project.
---

<!-- luma-foreman:generated from lumastack/luma-catalog/review-sweeps procedure/start-a-sweep. Regenerate with `luma-foreman apply`; edits are lost. -->

# Start a sweep

**Read `.luma/bundles/lumastack/luma-catalog/review-sweeps/procedure/start-a-sweep.md` and follow it.** That file is the procedure. This is the adapter that makes it reachable from here, and it deliberately carries no copy of it — the copy would drift.

Required reading from this bundle — open before following the procedure:

- `.luma/bundles/lumastack/luma-catalog/review-sweeps/policy/how-a-sweep-is-stored.md` — Where a review sweep lives, why it is backlog rather than a record, and the two units — the file that must be covered and the cluster actually reviewed in one go.

From the `lumastack/luma-catalog/review-sweeps` bundle, vendored at `.luma/bundles/lumastack/luma-catalog/review-sweeps/`. Do not edit anything under there — an adopted bundle is a copy, and editing it is drift.
