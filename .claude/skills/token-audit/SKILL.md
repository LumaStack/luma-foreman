---
name: token-audit
description: Measure one setup for token waste — memory files, tool deferral, model and effort, output filtering, subagents, schedule intervals and cache hit rate — and report it ranked by cost. Reports only; changes nothing. Use before deciding what to fix.
---

<!-- luma-foreman:generated from lumastack/luma-catalog/token-manager workflows/token-audit. Regenerate with `luma-foreman apply`; edits are lost. -->

# Audit token usage

**Read `.luma/bundles/lumastack/luma-catalog/token-manager/workflows/token-audit.md` and follow it.** That file is the workflow. This is the adapter that makes it reachable from here, and it deliberately carries no copy of it — the copy would drift.

From the `lumastack/luma-catalog/token-manager` bundle, vendored at `.luma/bundles/lumastack/luma-catalog/token-manager/`. Do not edit anything under there — an adopted bundle is a copy, and editing it is drift.
