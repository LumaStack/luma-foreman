---
name: update-bundle
description: Change a bundle's contents and version it correctly. Use when editing, extending, or fixing an existing bundle.
---

<!-- luma-foreman:generated from lumastack/luma-catalog/bundle-manager procedure/update-bundle. Regenerate with `luma-foreman apply`; edits are lost. -->

# Update a bundle

**Read `.luma/bundles/lumastack/luma-catalog/bundle-manager/procedure/update-bundle.md` and follow it.** That file is the procedure. This is the adapter that makes it reachable from here, and it deliberately carries no copy of it — the copy would drift.

Required reading from this bundle — open before following the procedure:

- `.luma/bundles/lumastack/luma-catalog/bundle-manager/policy/organizing-a-bundle.md` — The layout every bundle uses, what each directory is for, and what decides whether something is a document, an asset, or a type.

From the `lumastack/luma-catalog/bundle-manager` bundle, vendored at `.luma/bundles/lumastack/luma-catalog/bundle-manager/`. Do not edit anything under there — an adopted bundle is a copy, and editing it is drift.
