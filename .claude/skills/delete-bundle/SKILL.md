---
name: delete-bundle
description: Retire a bundle without breaking the projects that adopted it. Use when a bundle is superseded, wrong, or no longer maintained.
---

<!-- luma-foreman:generated from lumastack/luma-catalog/bundle-manager workflows/delete-bundle. Regenerate with `luma-foreman apply`; edits are lost. -->

# Delete a bundle

**Read `.luma/bundles/lumastack/luma-catalog/bundle-manager/workflows/delete-bundle.md` and follow it.** That file is the workflow. This is the adapter that makes it reachable from here, and it deliberately carries no copy of it — the copy would drift.

From the `lumastack/luma-catalog/bundle-manager` bundle, vendored at `.luma/bundles/lumastack/luma-catalog/bundle-manager/`. Do not edit anything under there — an adopted bundle is a copy, and editing it is drift.
