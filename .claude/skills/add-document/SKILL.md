---
name: add-document
description: Decide whether a document is needed, which one it is, and where it goes. Use when writing anything new, or when a document is outgrowing the file it is in.
---

<!-- luma-foreman:generated from lumastack/luma-catalog/project-documentation workflows/add-document. Regenerate with `luma-foreman apply`; edits are lost. -->

# Add a document

**Read `.luma/bundles/lumastack/luma-catalog/project-documentation/workflows/add-document.md` and follow it.** That file is the workflow. This is the adapter that makes it reachable from here, and it deliberately carries no copy of it — the copy would drift.

From the `lumastack/luma-catalog/project-documentation` bundle, vendored at `.luma/bundles/lumastack/luma-catalog/project-documentation/`. Do not edit anything under there — an adopted bundle is a copy, and editing it is drift.
