---
name: migrate-bundle
description: Move a bundle between catalogs, or restructure one in place, without stranding the projects that adopted it. Use when promoting, relocating, or reorganizing.
---

<!-- luma-foreman:generated from lumastack/luma-catalog/bundle-manager workflows/migrate-bundle. Regenerate with `luma-foreman apply`; edits are lost. -->

# Migrate a bundle

**Read `.luma/bundles/lumastack/luma-catalog/bundle-manager/workflows/migrate-bundle.md` and follow it.** That file is the workflow. This is the adapter that makes it reachable from here, and it deliberately carries no copy of it — the copy would drift.

From the `lumastack/luma-catalog/bundle-manager` bundle, vendored at `.luma/bundles/lumastack/luma-catalog/bundle-manager/`. Do not edit anything under there — an adopted bundle is a copy, and editing it is drift.
