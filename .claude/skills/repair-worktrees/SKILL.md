---
name: repair-worktrees
description: Diagnose and fix the states worktrees get stuck in — stale metadata, a branch that will not check out, a moved directory, a locked entry. Use when create or remove fails.
---

<!-- luma-foreman:generated from lumastack/luma-catalog/git-worktrees workflows/repair-worktrees. Regenerate with `luma-foreman apply`; edits are lost. -->

# Repair worktrees

**Read `.luma/bundles/lumastack/luma-catalog/git-worktrees/workflows/repair-worktrees.md` and follow it.** That file is the workflow. This is the adapter that makes it reachable from here, and it deliberately carries no copy of it — the copy would drift.

From the `lumastack/luma-catalog/git-worktrees` bundle, vendored at `.luma/bundles/lumastack/luma-catalog/git-worktrees/`. Do not edit anything under there — an adopted bundle is a copy, and editing it is drift.
