---
name: review-next
description: One slice — reconcile the index, orient without judging, let the reader read first, then act on what they say. Use to continue an open sweep, including after a break.
---

<!-- luma-foreman:generated from lumastack/luma-catalog/review-sweeps procedure/review-next. Regenerate with `luma-foreman apply`; edits are lost. -->

# Review the next unit

**Read `.luma/bundles/lumastack/luma-catalog/review-sweeps/procedure/review-next.md` and follow it.** That file is the procedure. This is the adapter that makes it reachable from here, and it deliberately carries no copy of it — the copy would drift.

Required reading from this bundle — open before following the procedure:

- `.luma/bundles/lumastack/luma-catalog/review-sweeps/policy/how-a-sweep-is-stored.md` — Where a review sweep lives, why it is backlog rather than a record, and the two units — the file that must be covered and the cluster actually reviewed in one go.

From the `lumastack/luma-catalog/review-sweeps` bundle, vendored at `.luma/bundles/lumastack/luma-catalog/review-sweeps/`. Do not edit anything under there — an adopted bundle is a copy, and editing it is drift.
