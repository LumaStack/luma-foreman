---
name: add-a-setting
description: Decide where a new configuration value lives and how strongly it binds. Use when introducing any value somebody might want to change.
---

<!-- luma-foreman:generated from lumastack/luma-catalog/luma-config procedure/add-a-setting. Regenerate with `luma-foreman apply`; edits are lost. -->

# Add a setting

**Read `.luma/bundles/lumastack/luma-catalog/luma-config/procedure/add-a-setting.md` and follow it.** That file is the procedure. This is the adapter that makes it reachable from here, and it deliberately carries no copy of it — the copy would drift.

Required reading from this bundle — open before following the procedure:

- `.luma/bundles/lumastack/luma-catalog/luma-config/policy/where-configuration-lives.md` — Two homes and one cache — what is committed, what belongs to the machine, and the test that tells them apart.

From the `lumastack/luma-catalog/luma-config` bundle, vendored at `.luma/bundles/lumastack/luma-catalog/luma-config/`. Do not edit anything under there — an adopted bundle is a copy, and editing it is drift.
