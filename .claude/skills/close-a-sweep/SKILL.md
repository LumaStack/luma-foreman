---
name: close-a-sweep
description: Finish or abandon a sweep honestly — check coverage, confirm nothing worth keeping is still trapped in the notes, then archive it. Use when the last unit is done, or when it is being stopped early.
---

<!-- luma-foreman:generated from lumastack/luma-catalog/review-sweeps procedure/close-a-sweep. Regenerate with `luma-foreman apply`; edits are lost. -->

# Close a sweep

**Read `.luma/bundles/lumastack/luma-catalog/review-sweeps/procedure/close-a-sweep.md` and follow it.** That file is the procedure. This is the adapter that makes it reachable from here, and it deliberately carries no copy of it — the copy would drift.

Required reading from this bundle — open before following the procedure:

- `.luma/bundles/lumastack/luma-catalog/review-sweeps/policy/how-a-sweep-is-stored.md` — Where a review sweep lives, why it is backlog rather than a record, and the two units — the file that must be covered and the cluster actually reviewed in one go.

From the `lumastack/luma-catalog/review-sweeps` bundle, vendored at `.luma/bundles/lumastack/luma-catalog/review-sweeps/`. Do not edit anything under there — an adopted bundle is a copy, and editing it is drift.
