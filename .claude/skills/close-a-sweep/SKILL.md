---
name: close-a-sweep
description: Finish or abandon a sweep honestly — check coverage, confirm nothing worth keeping is still trapped in the notes, then archive it. Use when the last unit is done, or when it is being stopped early.
---

<!-- luma-foreman:generated from lumastack/luma-catalog/review-sweeps workflows/close-a-sweep. Regenerate with `luma-foreman apply`; edits are lost. -->

# Close a sweep

**Read `.luma/bundles/lumastack/luma-catalog/review-sweeps/workflows/close-a-sweep.md` and follow it.** That file is the workflow. This is the adapter that makes it reachable from here, and it deliberately carries no copy of it — the copy would drift.

From the `lumastack/luma-catalog/review-sweeps` bundle, vendored at `.luma/bundles/lumastack/luma-catalog/review-sweeps/`. Do not edit anything under there — an adopted bundle is a copy, and editing it is drift.
